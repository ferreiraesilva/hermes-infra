# Bootstrap

This directory documents the reusable process for creating Hermes Agent installations. Bootstrap contracts must apply the shared baseline, validate installation metadata and preserve human approval gates.

Start with `new-installation.md`. Automation may replace manual steps later, but it must implement the same contracts and approval requirements.
