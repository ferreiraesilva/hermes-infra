# Scripts

This directory is reserved for small reusable validation and bootstrap scripts.

Do not add product-specific code. Add runtime scripts only when an approved shared requirement cannot be represented by configuration, schemas or documentation alone. Scripts must preserve human approval gates and provide clear validation output.
